#pragma once

#include <Windows.h>
#include <filesystem>
#include <thread>
#include <mutex>
#include <condition_variable>

class Minecraft {
public:
    int Launch() {
        return static_cast<int>(0);
    }
private:
    
};